version: "3.9"
services:
  db:
    image: mysql:8.0
    container_name: labdb-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: labpass
      MYSQL_DATABASE: labdb
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3307:3306"
    volumes:
      - dbdata:/var/lib/mysql
      - ./db.sql:/docker-entrypoint-initdb.d/db.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-plabpass"]
      interval: 5s
      timeout: 5s
      retries: 10

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: labdb-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: labpass
    ports:
      - "8081:80"
    depends_on:
      - db

  app:
    build: .
    container_name: lab9-10-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: labpass
      DB_NAME: labdb
      DB_PORT: 3306
      PORT: 5000
    ports:
      - "5001:5000"

volumes:
  dbdata:
